// two ways to declare array 
var arr1 = [];
var arr2 = [];
// - both declarations are same 
// - if we not give this data type and initialize empty array then defualt data type for array will be 'never'
var arr3 = [1];
// - if we are giving some initial values in array then it will figure out data-type by it self
